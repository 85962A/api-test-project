# 复制此文件为 .env，把 sk-xxxx 换成你自己的 Key（.env 已被 .gitignore 忽略，不会提交）
# 等号两边不要有空格；值不要加引号
DEEPSEEK_API_KEY=sk-xxxx

# 可选：换用其它 OpenAI 兼容服务（Kimi / 硅基流动 / 本地模型等）
# AI_EVAL_BASE_URL=https://api.deepseek.com/v1
# AI_EVAL_MODEL=deepseek-chat
# 用 OpenAI 时可直接写：
# OPENAI_API_KEY=sk-xxxx
